#include <stdlib.h>

struct Gradient {
    unsigned int size;
    unsigned int pos;
    float* arr;
    float stepSize; // maybe want an int for this?
};

void Train (Gradient *gradient, float coordinate, float dataPoint) {
    
    
    return;
}